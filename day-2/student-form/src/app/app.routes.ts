import { Routes } from '@angular/router';
import { MyFormComponent } from './STUDENT_FORM.component'; // adjust path

export const routes: Routes = [
  { path: '', component: MyFormComponent },
];

